MONGO_URI=
JWT_SECRET=
PORT=5000

# Optional Twilio real delivery
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TWILIO_WHATSAPP_FROM=

# Optional real email delivery (otherwise Email uses in-app demo mode)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# DishaAI
# Optional. DishaAI core scoring/chat works without an external AI key.
# Resume parsing requires: npm install
